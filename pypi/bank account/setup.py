from setuptools import setup

setup(
    name = 'vanks', # while installing pacakge, e.g., matplotlib...
    version = '0.0.1',
    description = 'Simple Bank Account.',
    long_description = open('Readme.txt').read(),
    url = 'https://pypi.org/user/imvickykumar999/',
    author = 'Vicky Kumar',
    keywords = ['account', 'bank', 'class', 'ideationology'],
    packages = ['vicks'], # while importing package, e.g., pyplot...
)
